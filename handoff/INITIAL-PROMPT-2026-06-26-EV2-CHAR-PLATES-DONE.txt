/session-anchors

=================================================================
WHAT WE ARE BUILDING (read these 5 lines first — this is the whole point)
=================================================================
- Product: a ~5-minute explainer FILM "Satu Keluarga, Satu Hari Penuh" for the Semesta Digital superapp (Indonesian market). It follows ONE family through ONE day.
- The family (4 mains): Herman (father ~46, clean-shaven), Ratna (mother ~42, hijab in public), Lisa (daughter ~20), Andi (son ~10).
- We produce the film as STILL keyframe images (a START frame + an END frame per shot). Erik generates every image in ChatGPT; Claude only AUTHORS the prompts and reviews. Later: Kling animates the pairs, then VO + After Effects.
- DONE so far: every character's reference "plate" set (the identity anchors) — all 4 families complete and committed.
- NOW: produce the FIRST scene, SEQ1-SC01 (the family waking at pre-dawn 04.40 in Herman's bedroom). Nothing about images is generated by you — you write prompts, Erik generates.

=================================================================
WHERE WE ARE (git truth)
=================================================================
- HEAD = c53bfc7 "Andi 12 plates DONE — ALL FAMILY MAINS COMPLETE". All plates on disk + committed.
- A prior restart of THIS task failed (model thrashed, lost the thread). That work was rolled back. You are starting clean from c53bfc7. Do not look for it.

=================================================================
THE ANTI-THRASH RULE (this is why the last attempt failed — obey it)
=================================================================
- Work in ONE bounded step per turn. Produce one artifact, present it, STOP, wait for Erik.
- If you ever feel unsure what to build: RE-READ `explainer-video-2/10-gateB-keyframes/SEQ1-SC01-RESTRUCTURE-2026-06-26.md`. The entire 7-shot plan is there. Do NOT improvise, do NOT re-derive the method, do NOT loop on deliberation.
- Lead each substantive reply with `Confidence: NN%` as conviction (one line, not mechanical). ONE recommendation, never a menu.
- You NEVER generate images. Erik does. You author + review + (on his "ya") rename the accepted file.

## Mandatory Skill Invocation
- `/session-anchors` (READ) at start: read CLAUDE.md, MEMORY.md, scope.md, context.md, lessons.md, this prompt's referenced docs.
- `/research-before-dispatch` before authoring any prompt or asserting file state — read the actual sheet/doc/render first.
- `/anti-sycophancy` + `/confidence` before any recommendation; `/brave` to not quit mid-step.

## You Are in Session EV2-SC01-PRODUCTION
Succeeds EV2-CHAR-PLATES-DONE (closed; all family plates committed, HEAD c53bfc7). Mandate: produce scene SEQ1-SC01 with the new family plates, following the locked 5-step pipeline + multi-angle shot grammar — ONE bounded step at a time.

## Goal
- **Session Goal:** SEQ1-SC01 rewritten to the 7-shot multi-angle grammar in `explainer-video-2/03-scene-detail.md` (`grep -E "SEQ1-SC01-SH0[1-7]" explainer-video-2/03-scene-detail.md` shows SH01–SH07, no "sarong" payoff line); `explainer-video-2/10-gateB-keyframes/SEQ1-SC01.md` carries master→derivative prompts for SH03 re-authored against the NEW family DAG plates; the `E01_kamar_bed4q.png` env-plate prompt authored; SH03 START+END regenerated, accepted, renamed → `scene-images/sc01/sc01_sh03_{start,end}.png`. Constraints that MUST NOT change: Claude never generates (Erik does); image-only (no Kling); within-shot camera-lock (angle+framing locked, only subject moves, START→END edit-in-place); angle+framing VARIED across cuts for richness; master=START full-prompt+attachment, derivative=END edit-in-place; report WHERE-prompt-lives + full ATTACHMENT-list after every prompt; commit only on explicit "ya".
- **Ultimate Goal:** finished ~5-min explainer — all keyframes under the locked method in story order, then Kling i2v + VO/AE + assemble.
- **Linkage:** SC01 is the pilot scene that proves the CUT-grammar + master→derivative pipeline with the new plates before the rest of the film.

## Bootstrap Reading (mandatory order — only these, do not over-read)
0. Read this prompt fully.
1. `explainer-video-2/10-gateB-keyframes/SEQ1-SC01-RESTRUCTURE-2026-06-26.md` — THE PLAN (7-shot table + execution order). Most important doc.
2. `scope.md` top block + `context.md` tail — current status.
3. `lessons.md` (2026-06-26 entries) — DAG, shot-recommendation rule, 5-step pipeline, per-cell-edit guard.
4. `explainer-video-2/03-scene-detail.md` § SEQ1-SC01 (the old 4-shot to rewrite) + `09-environment-sheet/E01-kamar.md`.
5. `explainer-video-2/08-character-sheet/H-herman.md` + `R-ratna.md` §3 (plates to attach).

## Verify State via Grep
```
cd /Users/eriksupit/Desktop/superapps
git log -1 --oneline                                              # expect c53bfc7
ls explainer-video-2/character-images/*/ | grep -c png            # expect 53 (H12 R17 L12 A12)
grep -cE "SEQ1-SC01-SH0[1-7]" explainer-video-2/03-scene-detail.md # currently 4 (old) → you make it 7
ls explainer-video-2/environment-images/E01_kamar/               # master,bed3q,nightstand present; bed4q NOT (you author it)
```

## After Bootstrap — YOUR FIRST STEP (one bounded artifact, then STOP)
Output ONE message: the **SEQ1-SC01 7-shot list as a single table** — per shot: angle (bed3q right-diag / bed4q left-diag / overhead / top-down), framing (long/medium/close), and the START→END subject delta. Take it from RESTRUCTURE §2 (do not invent a new structure). Present it for Erik's "ya". Then STOP — do not write files, do not author prompts yet. After "ya": write it into `03-scene-detail.md`, then proceed to the keyframe doc.

## The Locked Pipeline (5 steps — follow in order, one at a time)
1. Edit SC01 in `03-scene-detail.md` to the 7-shot grammar (after Erik approves the table).
2. Translate to `10-gateB-keyframes/SEQ1-SC01.md`.
3. Per shot write master→derivative: master = START full-prompt + attachment (Mode B); derivative = END edit-in-place (+attachment if needed). New angle at a cut = fresh full-prompt master.
4. Hand prompt to Erik → he generates → you review → on "ya" rename to `scene-images/sc01/` → next.
5. Execution order inside SC01: regen SH03 START+END → author+generate+accept `E01_kamar_bed4q.png` (HARD dependency) → pilot SH04 rear-view → SH05→SH06→SH07.

## After every authored prompt, report:
```
Lokasi: <file> → <section> → START|END
Attachments (N): 1. <file.png> — <role> … (or "none — edit-in-place on <render>")
```

## Special Concerns
1. RESTRUCTURE doc predates the character regen → its "current plates" now mean the NEW DAG set; old SC01 SH01–SH03 renders will be redone.
2. `E01_kamar_bed4q.png` (left-diag mirror of bed3q) is a HARD dependency — author/generate/accept BEFORE SH04.
3. SH03-END: Herman STAYS RECLINING when placing the phone (sit-up elided at the cut). State posture explicitly.
4. Bulk-editing a sheet's `reference_image`? PER-CELL only (`re.split(r'(?=^### )')`) + `json.loads` audit. A greedy block-regex corrupted Cell 6/7 before.

## Communication
- Erik generates all images + says "ya"/"approve"; you author/review, never generate. Informal Indonesian to Erik; English for all docs/prompts. Commit/push only on explicit "ya" (master branch).

## End-of-Session Closure Deliverables (KEYSTONE — non-optional)
Before closing, produce via `/handoff`: (1) next HANDOFF.md (+ `## Goal`), (2) next INITIAL-PROMPT.txt (+ `## Goal` + this Closure section), (3) governance sync (scope/context/lessons + auto-memory). Without these, the session is NOT closed.

## Anti-Flailing
- Tempted to generate an image → STOP (Erik's job).
- Tempted to author SH04 before bed4q exists → STOP (hard dependency).
- Tempted to do steps 1–5 in one turn → STOP; one bounded step, present, wait.
- Unsure what to build → RE-READ RESTRUCTURE doc; do not improvise.

Go — but your first output is ONLY the 7-shot table for approval.
