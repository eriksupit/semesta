────────────────────────────────────────────────────────────────────────────────────
INITIAL PROMPT — Ratna Character Sheet session (successor to GATE-0-Herman-complete)
────────────────────────────────────────────────────────────────────────────────────

## Mandatory Skill Invocation — Read FIRST
- anti-sycophancy — before any agreement/evaluation/recommendation/clarifying question.
- confidence — lead substantive replies with `Confidence: NN%`; >95% on Priority 1 or run Transparent Iteration.
- sepakat-atau-debat — when the user weighs an idea/claim.
- research-before-dispatch — before any state claim or consequential write; verify against live files, not memory.
- brave — when at risk of shortcutting/withdrawing/rubber-stamping.
- session-anchors — READ at open (context/lessons/scope) + WRITE on each validated decision.

OPERATING CONTRACT (CLAUDE.md) binds every turn: mutation needs explicit per-action "ya"; stay in scope; `Confidence: NN%`; <95% → research+loop, ONE recommendation no menu; CONVICTION not deference — no ritual approve/override/amend tag on pure-discussion turns; validation gate exists ONLY for mutation.

## You Are in: Ratna Character Sheet session (GATE 0 continues)
Predecessor closed with **Herman GATE 0 COMPLETE**: 9/9 reference plates approved in `explainer-video-2/character-images/H_herman/`. `07-style-reference.md` + `08-character-sheet/` (00-README + H-herman.md) authored. Nine token rules locked across canonical docs. Your job: author Ratna's character sheet using the SAME locked pattern — do not re-decide the rules.

## Goal
- **Session Goal:** `explainer-video-2/08-character-sheet/R-ratna.md` exists with: Ratna identity lock (FIXED block), 9 full t2i plate prompts (3 view × 3 shot) compliant with all 9 locked token rules, and a per-scene wardrobe table covering Ratna's 8 scenes; folder `explainer-video-2/character-images/R_ratna/` created. Check: `grep -c '"hair"' R-ratna.md` ≥ 10 AND `grep -cE "no |not in frame|high-key|graying|light-absorbing|non-reflect" R-ratna.md` = 0. Constraint: no locked token rule changes.
- **Ultimate Goal:** 5-minute explainer film + scene assets for ad-providers → pre-development funding sales tool.
- **Linkage:** Ratna is character 2 of 4 in GATE 0; finishing all character sheets unlocks GATE B (per-shot keyframe prompts) → GATE C → storyboard → film.

## Bootstrap Reading (mandatory order)
1. `CLAUDE.md` (OPERATING CONTRACT).
2. `scope.md` (STATUS LANJUT + NEXT sesi Ratna).
3. `context.md` (State AKHIR Sesi 2026-06-19 GATE B Prep — TERBARU block).
4. `lessons.md` (Insight Prompt Image Generation: positive-only, keyword-leak, uniform-not-gradient, matte plate).
5. `MEMORY.md` + memory `semesta-gate0-character-pipeline.md` (all 9 locked rules, durable).
6. `handoff/HANDOFF-2026-06-19-GATEB-PREP-RATNA.md` (this session's reference snapshot).
7. `explainer-video-2/08-character-sheet/00-README.md` (method + 9-cell template + all token rules — CANONICAL).
8. `explainer-video-2/08-character-sheet/H-herman.md` (clean template to replicate for Ratna).
9. `explainer-video-2/07-style-reference.md` (hijab/modest catalog §3.1/§3.2; anchor→character map §5).
10. `explainer-video-2/03-scene-detail.md` (Ratna's 8 scenes; read her prose for per-scene wardrobe).

## Locked token rules (DO NOT re-decide — replicate from H-herman.md)
1. Multi-angle = 9 separate images (3 view × 3 shot).
2. Aspect per shot: full=9:16, medium=2:3, close-up=4:5.
3. Camera-relative direction: Profile A=camera-left, Profile B=camera-right (mirror of A via reference upload). Compare full-res side-by-side before judging.
4. Positive-only: no negation; omit field if element cropped out.
5. Keyword-leak: no token whose dominant word drags an unwanted concept (light/sheen/reflect).
6. Uniform, not gradient (state positively).
7. Matte plate standard: hair `dry matte hair, velvety matte strands, chalky matte finish, loosely combed natural`; lighting `flat even soft diffused three-point studio key … matte skin and hair rendering` (no high-key). For Ratna hair is COVERED by hijab — apply matte to any visible hairline + skin.
8. Plate baseline: `plain solid white background, subject isolated`; photostock style; neutral grade; render-age below real (45 → ~`41 years old`).
9. Plate deviations: prime lens, no scene_depth/graphic, crew=costume/makeup/hair, portrait aspect.

## Ratna specifics
- 45, hijabi UMKM kuliner. Hair COVERED (Camille Friend anchor). Makeup Eryn Krueger Mekash. Costume Deborah Hopper. Hijab/modest catalog in `07 §3.1/§3.2`.
- Scenes (8): SEQ1-SC02, SEQ2-SC03, SEQ3-SC01, SEQ3-SC02, SEQ4-SC03, SEQ5-SC02, SEQ6-SC01, SEQ6-SC02.

## Per-plate validation flow
User generates externally → saves to `character-images/R_ratna/` → you read + align full-res + score rubric (aspect/view-direction/identity/wardrobe/background) → user "setuju" → you rename to `R_ratna_<view>_<shot>.png`. Profile B = mirror of Profile A via reference image. Error <5% accepted. Failures → `R_ratna/_rejected/`.

## After Bootstrap — STANDBY
Confirm scope in one sentence, put authoring of `R-ratna.md` to user, await "ya" before any write. Mutation needs explicit per-action approval.

## Anti-Flailing
- Do NOT re-decide the 9 token rules — they are locked; replicate H-herman.md.
- Do NOT write negation / gradient / keyword-leak tokens.
- Do NOT generate images yourself (user generates externally; you write + review + approve).
- Do NOT judge profile direction from a thumbnail — align full-res.
- Do NOT touch `Herman Model.png` (10th file, unexplained) without user clarification.

## End-of-Session Closure Deliverables (before next close)
1. Next handoff `.md` (with `## Goal` block) → `handoff/`.
2. Next initial-prompt `.txt` (this skeleton + `## Goal` + this section) → `handoff/`.
3. Update `00-WORKLOG.md` + `context.md`/`scope.md`/`lessons.md` per session-anchors.

Standby.
