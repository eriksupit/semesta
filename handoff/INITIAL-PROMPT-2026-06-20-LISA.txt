────────────────────────────────────────────────────────────────────────────────────
INITIAL PROMPT — Lisa Character Sheet session (successor to GATE-0-Ratna-complete)
────────────────────────────────────────────────────────────────────────────────────

## Mandatory Skill Invocation — Read FIRST
- anti-sycophancy — before any agreement/evaluation/recommendation/clarifying question.
- confidence — lead substantive replies with `Confidence: NN%`; >95% on Priority 1 or run Transparent Iteration.
- sepakat-atau-debat — when the user weighs an idea/claim.
- research-before-dispatch — before any state claim or consequential write; verify against live files, not memory.
- brave — when at risk of shortcutting/withdrawing/rubber-stamping.
- session-anchors — READ at open (context/lessons/scope) + WRITE on each validated decision.

OPERATING CONTRACT (CLAUDE.md) binds every turn: mutation needs explicit per-action "ya"; stay in scope; `Confidence: NN%`; <95% → research+loop, ONE recommendation no menu; CONVICTION not deference — no ritual approve/override/amend tag on pure-discussion turns; validation gate exists ONLY for mutation.

## You Are in: Lisa Character Sheet session (GATE 0 continues)
Predecessor closed with **Herman + Ratna GATE 0 COMPLETE**. Ratna: 12/12 plates approved in `explainer-video-2/character-images/R_ratna/` (9 uncovered turnaround + 3 hijab reference); sheet `R-ratna.md` v0.3; `00-README` manifest + `00-WORKLOG` updated. Nine token rules locked across canonical docs. Your job: author Lisa's character sheet using the SAME locked pattern — do not re-decide the rules.

## Goal
- **Session Goal:** `explainer-video-2/08-character-sheet/L-lisa.md` exists with: Lisa identity lock (FIXED block), **9 full t2i plate prompts** (3 view × 3 shot, uncovered baseline, hair worn LOOSE & down), and a per-scene wardrobe table covering Lisa's 6 scenes; folder `explainer-video-2/character-images/L_lisa/` (+ `_rejected/`) created. Check: `grep -c '"hair"' L-lisa.md` ≥ 10 AND `grep -cE "no |not in frame|high-key|graying|light-absorbing|non-reflect" L-lisa.md` = 0. Constraint: no locked token-rule changes; plates show hair loose (styling deferred to GATE B).
- **Ultimate Goal:** 5-minute explainer film + scene assets for ad-providers → pre-development funding sales tool.
- **Linkage:** Lisa is character 3 of 4 in GATE 0; finishing all 4 unlocks GATE B (per-shot keyframe prompts) → GATE C → storyboard → film.

## Bootstrap Reading (mandatory order)
1. `CLAUDE.md` (OPERATING CONTRACT).
2. `scope.md` (STATUS LANJUT + NEXT sesi Lisa).
3. `context.md` (State AKHIR Sesi 2026-06-20 — TERBARU block).
4. `lessons.md` (Insight GATE 0: modesty axis, keyword-leak in role, hair-loose plates, representative-covered-plates).
5. `MEMORY.md` + memory `semesta-gate0-character-pipeline.md` (all locked rules, durable).
6. `handoff/HANDOFF-2026-06-20-LISA.md` (this session's reference snapshot).
7. `explainer-video-2/08-character-sheet/00-README.md` (method + 9-cell template + token rules — CANONICAL).
8. `explainer-video-2/08-character-sheet/R-ratna.md` (clean template to replicate; ignore its §0/§3.A hijab parts — Lisa is not hijabi).
9. `explainer-video-2/07-style-reference.md` (Lisa anchors §5; wardrobe/hair/makeup deltas §3.1/§3.2/§3.3; footwear §3.4).
10. `explainer-video-2/03-scene-detail.md` (Lisa's 6 scenes prose for per-scene wardrobe).

## Locked token rules (DO NOT re-decide — replicate)
1. Multi-angle = 9 separate images; aspect full=9:16, medium=2:3, close-up=4:5.
2. Camera-relative: Profile A=camera-left, Profile B=camera-right (mirror of A via reference upload); files front/profileA/profileB, never left/right.
3. Positive-only (zero negation); omit field if element cropped.
4. Keyword-leak ban — including inside `character_identity.role` (the `hijabi` leak that forced a headscarf on Ratna). Keep Lisa's role free of unwanted-concept triggers.
5. Uniform-not-gradient; matte plate standard (no `high-key`).
6. Plate deviations declared (neutral grade, prime lens, plain white bg, crew=costume/makeup/hair, portrait aspect).
7. **Reference plates show hair LOOSE & down** to lock full hair identity; Lisa's half-up-clip / low-ponytail are SCENE looks for GATE B, not plates.
8. Render-age below real age. **Lisa render-age OPEN** — real 20; recommend ~19, verify against first plate before locking (flat −4 → 16 risks too-teen).

## Lisa specifics
- Tag L. 20, mahasiswi + content creator, young urban Jakartan. **Not hijabi → 9 plates only, no covered plates.**
- Anchors: costume Michael Wilkinson · makeup Judy Chin · hair Camille Friend.
- Makeup register: on-camera ready (`medium-coverage satin foundation … baked setting powder matte zero shine`), more polished than Ratna's natural.
- Footwear: `clean white low-top canvas sneakers, rubber sole`.
- Scenes (6): SEQ2-SC01, SEQ2-SC04, SEQ3-SC04, SEQ4-SC02, SEQ5-SC01, SEQ6-SC01.

## Per-plate validation flow
User generates externally → saves to `character-images/L_lisa/` (`Content <W>x<H>.png`) → you read + align full-res + score rubric (aspect/view-direction/identity/wardrobe/hair-loose/background-matte) → user "setuju" → you rename to `L_lisa_<view>_<shot>.png`. Profile B = mirror of Profile A via reference image. Error <5% accepted. Failures → `L_lisa/_rejected/`.

## After Bootstrap — STANDBY
Confirm scope in one sentence, put authoring of `L-lisa.md` to user, await "ya" before any write. Mutation needs explicit per-action approval.

## Anti-Flailing
- Do NOT re-decide locked token rules — replicate R-ratna.md / H-herman.md.
- Do NOT write negation / gradient / keyword-leak tokens.
- Do NOT generate images yourself (user generates externally; you write + review + approve).
- Do NOT judge profile direction from a thumbnail — align full-res.
- Do NOT add hijab/covered plates for Lisa (not hijabi).
- Do NOT touch `Herman Model.png` without user clarification.

## End-of-Session Closure Deliverables (before next close)
1. Next handoff `.md` (with `## Goal` block) → `handoff/`.
2. Next initial-prompt `.txt` (this skeleton + `## Goal` + this section) → `handoff/`.
3. Update `00-README` manifest + `00-WORKLOG.md` + `context.md`/`scope.md`/`lessons.md` per session-anchors.

Standby.
