Purpose & context
Erik is building "Semesta Digital" (working title, final name pending investor/stakeholder approval) — an integrated-marketplace superapp for the Indonesian market. Core differentiators: a fairness engine as the central operating principle, NU as a visual-only co-founder (strict no-verbal-mention policy; visual representation only), and a syariah opt-in mode. The project spans four verticals: goods marketplace, virtual office, financial trading, and education/masterclass channels.
Immediate production goal: an investor deck requiring 18 cinematic illustrations (S0–S17) generated via ChatGPT/GPT Image 2. Parallel asset: a 5-minute explainer video functioning as a sales tool for ad providers (logic chain: video → ad provider sees traffic potential → approaches brands → brands fund pre-development). The explainer video is hybrid AI scenes + live actor, featuring 7 characters.
Erik communicates in informal Bahasa Indonesia; all vault documents are written in English. Communication style is terse — "approve," "kunci," "go," "oke" mean full green-light, execute immediately, no re-confirmation needed.

Current state

TERBARU (2026-06-29c, SC01 PILOT PRODUCTION) — Active = explainer-video-2/ "Satu Keluarga, Satu Hari Penuh" GATE B keyframe production (text+image→image, IMAGE-ONLY; Erik generates in ChatGPT/GPT-Image-2, Claude authors prompts + reviews + on-"ya" renames the accepted PNG — Claude NEVER generates). **GATE 0 characters CLOSED + REGENERATED younger (2026-06-26)** via the DAG 12-cell method (closeup→medium→fullbody × front/profileA/profileB/rear; front_closeup=master; child CU Front = parent-face blend): all family plates accepted — Herman 12, Ratna 12 base + 5 hijab, Lisa 12, Andi 12 (in character-images/<TAG>_<char>/). **text+image→image METHOD DEFINITIVE** (doc `prompt-rules-text-image-to-image.md`, supports `prompt-rules-image-generation.md`): plate-state law (image-ref dominates text, cannot DELETE a baked object), subject-count ≤1 face-locked (+1 softened, ≥3 collapse), 3-mode grammar (A from-scratch / B identity-anchored / C composition-delta), camera-lock START/END (only Lapis-4 changes), **EDIT-IN-PLACE = deterministic camera-lock for END** (edit the START render with zero filename preamble + fidelity High + token-delta + `<x> unchanged`; a START is ALWAYS a full master prompt, never an edited render). **⭐ MASTER-PARENT + EDIT-IN-PLACE-ANGLE LAW (2026-06-29):** every scene/asset = ONE master (parent, full from-scratch prompt); a camera-ANGLE change = edit-in-place CHILD in the SAME context window (NOT a fresh full-prompt re-uploaded in a new window → resamples; ~6 bed4q renders failed). **+2026-06-29c refinement (resolves the false "SH04 conflict"):** a new-angle SHOT whose angle-PLATE already exists = **clone the prior accepted shot's full master prompt + swap the env angle-ref** ("match this vantage") + keep the layout master (Rule 19 dual-plate) — an angle-child, NOT a from-scratch master; distinguish producing the env angle PLATE (edit-in-place in-lane) from producing a SHOT at an existing-plate angle (full master clone + dual-plate). **SC01 PROGRESS:** SH01 (overhead CU eyes), SH02 (top-down phone), SH03 (right-diag bed3q medium 2-subject) — all START+END accepted + committed (`sc01_sh0{1,2,3}_{start,end}.png`; commits `4f2561e`/`912b17e`/`24ed7d8`). **SH04** (couple reclining, foot-of-bed `bed4q` vantage → Herman sits up) method RESOLVED = clone SH03-START + swap `E01_kamar_bed3q`→`E01_kamar_bed4q`; slot authored in `SEQ1-SC01.md`, STATUS ⬜ TO AUTHOR. SH05 = reach nightstand lamp → lamp ON. SC01 restructure 7→8 decided verbally, only the SH04 slot written so far. **OPEN (pending Erik):** git anomaly `D SEQ1-SC02.md` (deleted-from-disk, not this session — restore?) + one impulsive `lessons.md` bullet (revert/keep?); nothing committed without explicit "ya". **PRE-EMIT CHECKLIST A–H** added to `CLAUDE.md` (gitignored, local disk, read at bootstrap). Package: handoff/HANDOFF-2026-06-29b-EV2-SC01-SH04.md + INITIAL-PROMPT-2026-06-29b-EV2-SC01-SH04.txt. Detail: scope.md + context.md + lessons.md + auto-memory `semesta-text-image-to-image-method` (+2026-06-29c) + `semesta-gate0-character-pipeline` + `semesta-video-concept-rebuild`. The 2026-06-25 and earlier TERBARU blocks below are HISTORY — kept for trace, superseded by this block.

TERBARU (2026-06-25, STRATEGY REBOOT) — GATE B keyframe production for explainer-video-2/. Locked: SEQ1-SC01 (8/8) + SEQ1-SC02 SH01 pair (committed). SC02 SH02/SH03 BLOCKED after a 6-render failed loop on a 4-subject full-body shot. **The 2026-06-24 "build-asset-first / POLA BARU" doctrine is RETRACTED** — it held for zero-subject plates but failed once standing figures + an action were placed on a baked-object plate. **Governing lesson now: IMAGE-REFERENCE DOMINATES TEXT** — an attached plate is the floor of truth; text can ADD subjects/poses + re-light but cannot DELETE/override what is baked into the plate (proof: DOUBLE-CARPET render `26629cfc`, where a "Herman unrolls a carpet" beat drew a 2nd carpet over the plate's baked one; "bare floor" tokens did not remove it). Three structural variance drivers (next-session brainstorm agenda): (1) text bloat = secondary lever; (2) subject-count is dominant — 0–1 subject reliable one-shot, ≥3–4 collapses (blocking swaps, floor objects drop); (3) full-body WS of N people = max failure surface → default medium/close, full-body only when safe + for scene-developing. DECISION (Erik): stop token-firefighting; brainstorm the text+image→image method in a NEW session + author vault-root doc `prompt-rules-text-image-to-image.md` that SUPPORTS `prompt-rules-image-generation.md`. Prayer-surface trilemma SOLVED at PLATE level (single plain green prayer carpet plate `E02_ruangkeluarga_reverse_prayer.png` + subuh variant, accepted) — do NOT reopen; the open problem is the METHOD of placing figures on it. Package: handoff/HANDOFF-2026-06-25-EV2-PROMPTING-METHOD-BRAINSTORM.md + .txt. Detail in lessons.md (2026-06-25 retraction entry) + auto-memory semesta-image-prompt-craft.md.

TERBARU (2026-06-21, end of GATE B / GATE 0-ENV session) — Active work = the 5-minute explainer video "Satu Keluarga, Satu Hari Penuh" in explainer-video-2/, cast of 4 (Herman 50, Ratna 45, Lisa 20, Andi 10). GATE A (scene detail) COMPLETE (6 seq / 19 scene / 57 shot / ≈312s). **GATE 0 (character plates) CLOSED — 53 plates approved**: Herman 9, Ratna 12, Lisa 9, Andi 9, figurans 14. **GATE B (per-shot keyframe prompts) standard LOCKED** (00-WORKLOG GATE B entry): WARM color_grade token = "Roger Deakins palette, lifted blacks medium gray, neutral shadows, clean whites, Kodak 2383 emulation, slightly warmer midtones" (warmth from color_grade ONLY, garments desaturated); video crew = Cuarón / Lubezki / Caballero; per-shot start/end pairing (end-N = start-N+1); new field character_identity.identity_reference binds the plate file to the ChatGPT attachment; validated on SEQ1-SC01-SH03 over 6 iterations (HP rear-to-camera, off-axis anti-AI-cliché angle, expression as a sub-object for a beat like just-woke, subject-on-the-bed spatial fix). **ADDENDUM A1 (canonical 00-WORKLOG) — ALL graphics removed from AI generation**: app UI / ad cards / charts / super / motion graphics done in After Effects by a human animator; Lapis 5 (on_screen_graphic/graphic_proximity/ui_overlay) DROPPED from every keyframe prompt; devices appear but screens render off/neutral, comp-aware; cross-linked in prompt-rules + 05 + 03 + lessons + auto-memory semesta-no-ai-graphics-addendum.md. **GATE 0-ENV (environment reference plates) IN PROGRESS** — method `09-environment-sheet/00-README-ENV.md` + `environment-images/` created; 14 unique environments E01–E14 (6 home rooms share one architecture anchor + 8 outside); per-env scoped-angle (not 360), empty-room, neutral grade, ADDENDUM A1; THREE mandatory anchors per env prompt: owner social class in `scene` + INTERNATIONAL brand register (IKEA etc. as "<brand>-style register" not logos; model localizes via Jakarta tokens) + INTERNATIONAL interior-designer pair (production_designer Eugenio Caballero + interior_designer Ilse Crawford) + NAMED public-domain artwork for cross-angle consistency (home default: Hokusai "The Great Wave off Kanagawa" + "Fine Wind Clear Morning Red Fuji"). E01 Kamar Herman master v3.1 done (compliant). NEXT (continued in handoff/INITIAL-PROMPT-2026-06-21-ENV.txt): finish E01 angles (bed3q/wallart/nightstand) → E02–E14 → return to GATE B keyframes. **Hard behavioural lessons this session (do NOT repeat — full detail lessons.md §Koreksi GATE 0-ENV): (1) env prompts MUST be authored FROM prompt-rules + the approved character-plate formula (H-herman.md Cell 1), never from memory — last session used negation + invented framing/angle/camera/lighting tokens + omitted subject_anchor; (2) env = object/space scene → use subject_anchor, library tokens, positive-only, valid time_of_day; (3) keep env empty+neutral but LIVED-IN family register, NOT clinical/catalogue/architectural (that reads as showroom/hotel); (4) multi-angle env consistency uses the CHARACTER method = reference-image upload of the master + explicit distinct camera spec (NOT text-only, which mirrors the room; NOT "match the plate" without a new-angle spec, which clones it); (5) do NOT generalize "t2i can't do consistent multi-angle" — 53 character plates prove it can; defeatism/goal-minimal violates brave.** The S0–S17 investor-deck illustration record below is COMPLETE and ARCHIVED — still valid for that deck, superseded as the active task.

Illustration pipeline: 14 of 17 scenes approved (S0–S12 + S12b — updated 2026-06-10 Sesi 12). Deck grew to 17 scenes (S12b = ADDED: elder pausing for worship, sharia opt-in / values-aware feature; companion to S12). S15 (post-production composite) and S16 (typography only) are SKIPPED. Deck illustrations COMPLETE: 19/19 (S0–S14 + S12b + S14b + S17 + S17b). S17b = female mirror closer of S17 (M/F bookend). Lesson: a companion scene's super must REINFORCE, not duplicate, its pair's super (S17 = structure "One Ecosystem./One Set of Rules for Everyone"; S17b = human payoff "A Place for Everyone./Belonging by Design"). S15/S16 removed from deck (video-only beats, video docs untouched). Claude Design handoff DONE (2026-06-10): handoff/HANDOFF-CLAUDE-DESIGN-2026-06-10.md (upload via "Upload a doc") + handoff/INITIAL-PROMPT-CLAUDE-DESIGN-2026-06-10.txt (paste in chat). Adapted to the Claude Design UI: it cannot read a filesystem, so the handoff is self-contained and uses a dual-key image manifest (filename + content description) for the 19 attachments — zero filepath references. Special tasks baked in: S6 Rp 340.000 card composite, optional ecosystem montage from the 7 character images, S16 as native typography, financials only from the operator's uploaded copy doc. Deck illustration project COMPLETE (19/19) + handoff ready. S14b approved (17/18): added hijabi-ibu culinary-for-business scene filling the middle-aged-hijabi representation gap (grep confirmed zero hijab before); new rule — a device screen facing the subject (back to camera) must NOT embed UI; show its content as a FLOATING screen-preview card. Deck grew to 18 scenes (ADDED S12b + S14b). After S17 → 18/18 → Claude Design handoff. S14 approved (16/17): corporate two-person wall-display interaction, finger on large screen clean, negations removed, garment specified, off-axis added. S13 approved: turn-back portrait + active pose + flipped subject-right/graphic-left, because slight off-center alone does NOT break presenter monotony — vary angle+pose+side together. After all approved → Claude Design handoff (PNGs + Section 1–4 of 08). Key new rules locked this session: (1) unspecified visible garment + ethnic token → model auto-fills batik; specify every garment affirmatively, correct by specification not negation (validated S12 A/B). (2) Two graphics in one frame is valid — diegetic ID push-notification banner + English investor frosted card — if you differ the visual TYPE and separate positions (validated S12b); never two twin frosted cards. (3) Abstract/devotional meaning must not rest 100% on text — add a non-text visual cue. S12b naskah added to 06-storyline.md (Beat 2b). S10 lesson: large screen + finger-on-screen is safe from hand-melt (inverse of small-phone S6); Teknik 3 embedded dashboard + Teknik 2 floating card both legible on one large monitor; "two people reading the same screen" = strong B2B interaction. S11 lesson: presenter full-body validated in a populated open operations floor — "rebuild solo workspace → populated professional floor" (S5/S7) pattern holds; every presenter scene must use a different populated room. S12 pre-patched: dead-center→off-axis low-angle OTS, tablet face-grid softened to avoid melt. DECK TONE STANDARD (locked): every color_grade = `cool midtones desaturated, cool-dominant zero amber push`; all ungenerated prompts already rolled to it. Super/graphic text must be UNIQUE per scene (no duplicates). Always verify a scene's APPROVED marker against the actual PNG in `rough-presentation-image/` — text markers can go stale.
Workflow: Erik generates images externally in ChatGPT/GPT Image 2 and uploads results; Claude writes, iterates, and approves prompts. Approved images are moved to explainer-video/rough-presentation-image/.
Session bootstrap: Read CLAUDE.md → MEMORY.md → scope.md → context.md → lessons.md → latest HANDOFF-*.md before any response. Next session initial prompt: handoff/INITIAL-PROMPT-2026-06-29b-EV2-SC01-SH04.txt (latest authoritative handoff).
Active living documents: prompt-rules-image-generation.md (root vault, single source of truth for all prompt rules), prompt-rules-text-image-to-image.md (canonical text+image-to-image method; use as the replacement if `semesta-text-image-to-image-method.md` is requested but not found), and explainer-video/08-investor-deck-brief.md (Section 5 contains all 18 prompts, currently at v1.4+). **SOP (mandated 2026-06-22): every prompting insight/lesson that PROVES SUCCESSFUL on a real generated image MUST be recorded in prompt-rules-image-generation.md — the canonical doc — not only in lessons.md/per-asset sheets; add with a "(Validated <date>, <evidence>)" trace. A validated universal rule living only in a side doc is a documentation gap. Canonical in CLAUDE.md §PROMPT-CRAFT SOP + auto-memory semesta-prompt-rules-sop.md.**


On the horizon

Complete S5–S17 illustrations; all 18 approved images become the asset base for a Claude Design session to build the full presentation deck.
Final project name confirmation pending investor/stakeholder input.
RAB platform AI totaling ~$1,214 USD: Midjourney Pro, Kling Premier, Runway, Seedance/fal.ai, Suno, Claude Max 20x, ChatGPT Pro, Gemma 4/OpenRouter — all paid tiers.


Key learnings & principles

Image model spatial logic: Models read spatial positioning like CSS/compositing logic, not human directional language. Floating UI overlays require CSS-style tokens (center-x aligned, pixel offsets, dimensions, z-index) — not "above/below/left/right."
Culturally specific labels fail in image models: Terms like "Nahdliyin" are not recognized; replace with concrete facial/body feature descriptions and broad ethnic anchors (Javanese commoner, Sundanese, mixed urban Indonesian).
BANNED prompt tokens: "amber-ochre shadows," terracotta wardrobe, "HD makeup"/"HD setting powder" (triggers high-contrast specular highlights/oiliness drift). Yellow tone drift caused by warm shadow + warm wardrobe combinations.
Negation tokens are prohibited in all prompts — token strings only, no negations.
Angle anti-cliché: for live-actor full-body scenes, never use "subject centered frame" + eye-level (standard presenter look). Push subject to right/left-third, camera low to one side, let the environment recede on a diagonal as leading lines (validated S3).
Color-grade one-temperature rule: never combine two midtones-temperature tokens in one color_grade — a warm token ("slightly warmer midtones") plus "cool-dominant zero amber push" lets warm win and the frame goes golden (S3 iteration-1). Pick ONE; for night/indoor-warm-practical scenes match S2 exactly: "cool midtones desaturated, cool-dominant zero amber push".
Cross-scene tone consistency is judged by comparing directly against the previously-approved PNG, not per-scene in isolation.
No confabulation in vault writes: append-not-overwrite, specificity over generality, negative lessons treated as first-class entries.
Filesystem MCP write reliability: str_replace fails silently on non-ASCII characters (em-dashes, special quotes). Reliable pattern: read_file full content → rebuild complete file → write_file entire file → verify with read_text_file using head parameter.
Binary files cannot be written via Filesystem MCP write_file (string-only). Binary delivery requires present_files for manual download.
Bash tool runs in cloud container, not on the Mac filesystem — writes via bash_tool do not reach the vault. Only Filesystem MCP write_file reliably writes to the Mac.
Session memory is continuous, not end-of-session: validated preferences, corrections, and new insights must be written to vault immediately, never deferred.
Seedance 2.0: technically superior but requires fal.ai third-party API wrapper for real human face workflows. oMLX: rejected for this project (requires local model storage + minimum 16GB unified memory, MacBook spec unconfirmed). Gemma 4: vision-language model (text output only), functions as prompt reasoning layer feeding Midjourney — not a text-to-image generator.


Approach & patterns

Single recommendation + one approval gate: never offer option menus; always close with one clear recommendation and one approval trigger.
Defensible over optimistic: budget figures and claims must be defensible, not optimistic.
Prompt standard — 6-layer architecture (defined in prompt-rules-image-generation.md):

Layer 1: mood / color_grade / style
Layer 2: scene / location / time_of_day / atmosphere
Layer 3: character_identity + character_style — FIXED
Layer 4: action / pose / gesture / expression — VARIABLE
Layer 5: graphic (optional; floating UI uses on_screen_graphic + graphic_proximity with CSS-style tokens)
Layer 6: framing / camera / lighting + 6 crew pairs (director, lighting_director, production_designer, interior_designer, costume_designer, makeup_artist)
Format: token strings, no negations, C2 adjective-dense


Character briefs: structured JSON with explicit fields for face, body, clothing, environment, palette, style modifiers.
Session close protocol: every session must produce a handoff document + initial prompt in handoff/, and update context.md, scope.md, lessons.md, Superapp — Home.md.
/session-anchors skill: governs all reads/writes to the four memory anchor files. Protocol A = READ at session open; Protocol B = WRITE continuously on any validated preference/correction/insight; Protocol C = FULL CLOSE with handoff + initial-prompt artifacts. Co-invokes /confidence, /anti-sycophancy, /brave, /research-before-dispatch.


Tools & resources

Obsidian vault: /Users/eriksupit/Desktop/superapps/ — single source of truth, connected via Filesystem MCP

Superapp — Home.md (hub)
knowledge-base/ (Executive Brief, Ikhtisar Naratif, Proyeksi NU)
explainer-video/ (00-charter, 01-concept, 02-production-brief, 03-rab, 04-pipeline-ai-production, 08-investor-deck-brief, rough-presentation-image/)
handoff/ (HANDOFF-.md and INITIAL-PROMPT-.txt artifacts)
raw-kb/
context.md, scope.md, lessons.md (root)
prompt-rules-image-generation.md (root)
.claude/skills/session-anchors/SKILL.md


Image generation: ChatGPT Pro / GPT Image 2 (primary illustration pipeline)
AI video: Kling Premier, Runway Gen-4.5, Seedance 2.0 via fal.ai
Prompt reasoning layer: Gemma 4 via OpenRouter
Music: Suno AI
Primary AI assistant: Claude Max 20x
PDF generation: ReportLab (Python, bash environment); output to /mnt/user-data/outputs/
Filesystem access fallback: when MCP connector is inactive, upload files directly (accessible at /mnt/user-data/uploads/[filename])
---
GATE 0-ENV prompting (E02 Ruang Keluarga, 2026-06-23, validated live) — see auto-memory `semesta-env-prompt-craft.md` + `prompt-rules-image-generation.md`:
- Named-product VERIFIABILITY beats name-presence: weak silhouette → model improvises. KALLAX `2x2 open square cube grid` (countable) + BESTÅ `low closed flat-door media cabinet` (vs LACK which rendered as ambiguous cubes). Keep distinct silhouettes per piece.
- Camera-relative direction for fixed-camera env plates: `camera-LEFT/RIGHT/far BACK wall` (abstract `opposite/facing` failed 4 renders).
- Distinct named public-domain artwork per room: E01 Hokusai Great Wave vs E02 Hokusai Red Fuji (same register, distinct image).
- Enumerate EVERY surface accessory (keyboard, mouse) or render is inconsistent across plates.
- Master = reference anchor: if spec gains an object, regenerate the MASTER first, then re-derive dependent angles (Erik correction).
- Token-per-token: no `and/for/with/bearing` inside JSON token values; comma clusters only (b.119). Negation ban absolute.
- Each derivative angle must earn distinct framing (F floor-seat first came back a near-duplicate wide → tightened to seated-height MS).
---
GATE B keyframe authoring (SEQ1-SC01 pilot, 2026-06-23, Erik correction) — canonical in `prompt-rules-image-generation.md` (§Struktur 6 Lapis) + `explainer-video-2/10-gateB-keyframes/README.md`:
- **Attachment-line in EVERY generate unit.** Each keyframe = ONE complete standalone prompt carrying its own `environment_reference` (room plate) + `character_identity.identity_reference` (face plate), naming the attached file. A start/end pair = TWO complete prompts (identical except Lapis-4), each repeating the attachment lines. Do NOT split into "fixed-spec + Lapis-4 variant" and rely on mental merge — the model reads each unit alone and won't know which attachment to bind if the line sits only in a separate fixed block. SOP source: `09-environment-sheet/E01-kamar.md:61` ("COMPLETE copy-paste-ready, zero combine").
- Per-shot authoring checklist: camera direction (held phone = rear-to-camera/screen-to-subject; lying-phone insert = top-down, screen OFF per ADDENDUM A1); contextual waking state (groggy/kucel in Lapis-4 — heavy lids, sleep-creased, bed-head — identity still from the plate); screen-glow on face simulated via `lighting`, not a lit screen in frame.
- **END frame anchors on the START render (validated 2026-06-23, SEQ1-SC01, Erik correction).** The END of a start/end pair (Kling i2v) is generated by attaching the already-generated START image as composition anchor (`composition_reference` = the START render file) + the face plate; prompt reduced to minimal-change (composition/head-position/framing/lighting identical, only Lapis-4 changes). Never generate END from neutral plates alone — composition won't match START → i2v warp. START is born from plates; END is born from the START render. Same for a chained start flowing from the prior shot's end.
- Over-bed eyeline: a subject "looking at the ceiling" on an over-bed angle must be written as gaze "into the overhead camera lens" (camera occupies the ceiling vantage), else eyes drift off-frame. Framing for a reclining figure + env-reference renders wider → set token to MCU, don't fight it.
- **🔒 TRIAD ATTACHMENT MANDATORY (Erik mandate 2026-06-23):** every shot frame (START & END) MUST attach AND name in-prompt all three: (1) CHARACTER plate(s) for every person in frame, (2) ENVIRONMENT plate (`E01_kamar_*`), (3) START/END-reference (direction plate / render anchor). Even when the START/END render already shows the environment, the environment plate is STILL mandatory — anchor render alone is insufficient, especially for an object given an action (Herman placing phone on nightstand → without the nightstand plate the model hallucinates the surface). No exceptions, including anchored END frames. **ENFORCEMENT (pre-emit cross-check, Erik 2026-06-23):** recording the rule ≠ applying it — before declaring any frame ready, audit that every name in the prompt (each `identity_reference` person, each `environment_reference` object/room) has a matching attached plate; reclining person = full/medium plate, not closeup. Run this audit yourself per frame; do not wait for the user to catch the violation.
- **SEQ1-SC01 pilot techniques (validated 2026-06-23, full canon in `prompt-rules-image-generation.md`):** (1) SUBJECT MUST BE IN THE PROMPT (Lapis-3) — attachment is layer-2 and cannot inject a person/object the prompt omits (proof: SH02 with no human tokens rendered empty despite a couple in the lighting-ref). (2) SCREEN-GLOW ON FACE = screen ON + phone rear-casing-toward-camera (real glow, UI hidden from lens, UI to AE) — never "screen off + fake glow". (3) Tight reclining shot = CU/ECU not MCU (reclining figure + env-ref always renders wider; spec one stop tighter than target). (4) Over-bed eyeline depends on camera: only if truly overhead does gaze=lens; diagonal-above → gaze "up to the ceiling" (check the render). (5) Cultural terms: use the strong-English-prior word where one exists (prayer mat/headscarf/sarong); for niche untranslatables (mukena/peci) a single local token beats a generic multi-word English phrase (which is multi-interpretation → spawns stray objects). (6) Model improvisation = a prompt gap — close with affirmative closed-enumeration AND make the spec match the canon object count (removed clock, one nightstand, single wall artwork). (7) Pre-dawn "subuh" = very-low-key cool-blue in the `lighting` layer (warm stays in `color_grade`). (8) Regen-on-prompt-change ONLY when the visual target changed (de-negation/synonym that preserves the target → don't regen). (9) Two-character scene = symmetric `character_identity_husband`/`_wife` keys, never plain + suffixed mix.
---
GATE B keyframe SEQ1-SC02 (sholat subuh, 2026-06-24, canonical in `prompt-rules-image-generation.md` changelog + `10-gateB-keyframes/SEQ1-SC02.md`):
- **CANVAS-PIXEL SPATIAL GRAMMAR FOR FIGURES (validated 2026-06-24, SH02-START).** Relative placement terms (`side by side`, `spaced inward`, `clear of the sofa`) keep getting improvised — a back-row figure kept landing on the sofa across 4 renders. FIX (worked first try): declare a camera-frame canvas (16:9, 1920px ref) + give each figure/mat an explicit `center-x ≈ Npx`, a central band (`x 680–1240`), a margin to each edge (`≥430px`), and pin off-band furniture (`sofa beyond x 1500 camera-right kept outside the figures' band`). Extends the existing `graphic_proximity` CSS-overlay rule to PEOPLE. Use px coordinates for any multi-subject horizontal layout; stop using `side by side`/`inward`.
- **Reverse-angle env plate when one camera axis goes monotonous + crowds a wall.** SC02 was stuck on the E02 master axis (no angle change after the lamp; makmum cramming the TV wall). Built a NEW reverse plate `E02_ruangkeluarga_reverse.png` (Cell R) — camera backs onto the window, looks into the room (sofa→camera-right, TV→camera-left, doorway+KALLAX far wall). Prayer now faces the window/camera = faces visible, no cramming. Reverse cell deviates from "paste §1 verbatim" (camera-relative sides flip); consistency held via master upload as reference-image. Character plates switch profileA→`front` for a face-camera shot.
- **Saf fikih + per-person sajadah:** congregational order imam → boy → women (Herman→Andi→Ratna,Lisa); each makmum on their own prayer mat; four sajadah in a column on the rug.
- **Pre-prayer beat ≠ prayer posture:** the mat-laying/gathering shot = makmum STANDING relaxed + chatting + warm smiles (positive tokens, zero negation), NOT arms-folded; formal qiyam/takbir is the later shots. `crouching` on the imam + `devotional quiet eyes lowered` on makmum made them render seated/solemn — fix with explicit `standing relaxed, chatting, light happy smiles`.
- **Modesty per-beat within one scene:** Ratna SH01 (pre-prayer, mahram-only) UNCOVERED; mukena only from the prayer beat (costume change across the cut). Don't blanket one wardrobe across a scene that crosses the modesty trigger.
